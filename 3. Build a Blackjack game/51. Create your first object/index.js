// Create an object that represents an airbnb castle listing.
// It should contain at least one boolean, one string, one number, and one array
// Log out at least two of the keys using the dot notation


let airbnbCasletListing = {
    name: "Enchanted Castle",
    pricePerNight: 450,
    isAvailable: true,
    amenities: ["WiFi", "Pool", "Breakfast", "Guided Tours"]
}

console.log(airbnbCasletListing.name)
console.log(airbnbCasletListing.pricePerNight)

// // for (let key of airbnbCasletListing.amenities) {
// //     console.log(key)
// // }

// // for ( let tag of course.title ) {
// //     console.log( tag )
// // }

// for (let key in airbnbCasletListing) {
//     console.log(airbnbCasletListing[key])
// }

console.log(airbnbCasletListing.name)